# Generara un sample a partir de un dado de seis caras
# y comprobar si cuando se asigna a una variable 
# se vuelve a generar o no
die <- 1:6
dice <- sample(die, size = 2, replace = TRUE)


# No genera nuevos valores